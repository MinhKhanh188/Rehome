name: mainExpected

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  mainExpected:  # 👈 job name must match the required check
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Passing check for branch protection
        run: echo "This check is here to satisfy GitHub rules."
